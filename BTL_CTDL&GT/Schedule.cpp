#include <Schedule.h>
#include <iostream>
using namespace std;
//Hàm khởi tạo danh sách Schedule
ScheduleNode* initScheduleList(){
    return NULL;
}

//Hàm thêm lịch khám mới
void addSchedule(ScheduleNode*& head, AppointmentNode*& appointmentList, int appointmentID, string date, string time)